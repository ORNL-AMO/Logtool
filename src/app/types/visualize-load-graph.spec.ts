import { VisualizeLoadGraph } from './visualize-load-graph';

describe('VisualizeLoadGraph', () => {
  it('should create an instance', () => {
    expect(new VisualizeLoadGraph()).toBeTruthy();
  });
});
